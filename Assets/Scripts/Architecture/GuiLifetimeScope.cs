using VContainer;
using VContainer.Unity;

namespace DesignPatterns.Architecture
{
    public class GuiLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            
        }
    }
}